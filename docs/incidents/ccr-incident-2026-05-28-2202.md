# 📋 CCR-Server 故障报告

**报告窗口**: 2026-05-27 22:02:13 ~ 2026-05-28 22:02:13 (Asia/Shanghai)
**报告生成**: 2026-05-28 22:02
**数据来源**: `claude-relay-error-2026-05-27.log.1.gz` + `claude-relay-error-2026-05-28.log` + main log (`.12.gz`–`.16`)
**数据覆盖说明**: 主日志（全量流）早于 2026-05-28 17:31 的 rotation 已被清理，慢请求统计仅覆盖 17:31–22:02 这 4.5 小时窗口

---

## 一、TL;DR

17:00–18:00 出现高峰压力，"Others" 账号池耗尽，触发 429 次选账号失败，同时带来大量慢请求（avg 22s，max 617s）和客户端断连；Anthropic 上游全程 **无 529 过载**，故障根因是本地账号池容量不足以应对峰值并发，自动退避机制吸收了压力但未触发 webhook。

---

## 二、错误总览

| 数据集 | 行数 | 时间范围 |
|---|---|---|
| error log (过滤后) | 1,649 | 2026-05-27 22:02 → 2026-05-28 21:21 |
| main log warn+error (过滤后) | 88,404 | 2026-05-28 17:31 → 2026-05-28 22:02 |

---

## 三、故障分布与归因

### 3.1 上游 Anthropic 过载 (529) — **0 次**

整个 24h 窗口内 **零 529**，Anthropic 服务正常。

### 3.2 中转返回 500 (账号池耗尽) — **203 次**

全部为 `No available accounts in group Others`（195 次消息接口 + 8 次 token_count 接口）。

**受影响 API Key（Top 4）：**
| 次数 | Key |
|---|---|
| 85 | Limited |
| 63 | 黄军 |
| 38 | 李伟 |
| 17 | 覃国良 |

**按小时（账号选择失败总量，含内部重试）：**
| 小时 | 失败次数 |
|---|---|
| 17:xx | **429** ← 峰值 |
| 14:xx | 114 |
| 18:xx | 66 |

### 3.3 客户端断开 (Claude relay error) — **411 次**

无 error body，均为流式传输中途客户端主动断开，不代表服务端故障。分布如下（集中在峰值时段）：

| 小时 | 次数 |
|---|---|
| 17:xx | **194** |
| 18:xx | 57 |
| 14:xx | 50 |
| 13:xx | 20 |
| 09:xx | 18 |
| 其余各小时 | ≤15 |

### 3.4 上游账户级 Stream / API 错误 — **196 次**

**Stream 错误 (140 次) — 按账号：**
| 次数 | 账号 |
|---|---|
| 26 | Reuben |
| 15 | Shuting |
| 13 | rnd1.two |
| 11 | rnd4.two |
| 10 | rnd.ai2 |
| 9 | rnd.ai0 |
| 8 | Zeming / rnd2.two / Bryan.Zhao |
| ≤7 | 其余 rnd.* 账号 |

**API 请求错误 (56 次)：** 账号 `129869b2`（UUID）占 52 次，可能是某 CCR/Droid 类账号稳定性问题；另有 3 个账号各 1–2 次。

**400 Bad Request (30 次)：** 15 次来自 stream relay（`claudeRelayService.js:2644`），15 次来自非流式，属于客户端传参错误（非账号问题）。

**503 Service Unavailable (8 次)：** 4 次 stream + 4 次非流式，单次上游抖动，未持续。

### 3.5 慢请求 — **1,679 次**（仅统计 17:31–22:02 窗口）

| 指标 | 值 |
|---|---|
| 总次数 | 1,679 |
| 平均耗时 | **22,374 ms (22s)** |
| 最大耗时 | **617,123 ms (10.3 min)** |
| 峰值小时 | 18:xx (897 次) / 17:xx (635 次) |

慢请求与账号池耗尽高度重叠，大概率是请求在排队等待可用账号时积压。

---

## 四、其他告警

### 4.1 业务级

**相似度拒绝 (249 次)** — 系统提示词相似度低于阈值 0.5（观测到 0.4292–0.4837），Key 字段为空（可能是匿名或配置问题的请求）。分布在全天各小时，无明显峰值。

**日成本超限：**
| 次数 | Key |
|---|---|
| 6 | 魏玉峰 |
| 3 | 徐鬂 |

**Key 级转发失败（李居昌 52 次）** — 全部集中在 17:41–17:42，与峰值时段高度重叠，推测是峰值期间连续遇到账号池空或错误导致批量失败。

### 4.2 请求中断 (Unhandled Error)

**58 次** `BadRequestError: request aborted` — 全部来自 `192.168.20.1`（claude-cli 2.1.150），客户端在请求体传输完成前断开。属客户端行为，非服务端问题。

### 4.3 安全/扫描

**Invalid API key 尝试：无**，**404 扫描行为：无**。

### 4.4 配置/环境

Token 刷新失败：**无**。

---

## 五、时间线 (Asia/Shanghai)

```
May 27
22:02  窗口开始；少量 client disconnect (7次) 和相似度拒绝 (7次)
23:xx  基线静默期，零星 400 错误

May 28
00:xx  4 次 client disconnect，零星 400
02:xx  首批 400 stream error（rnd1.two 账号）
04:xx  轻微 client disconnect
08:xx  58 次 Unhandled abort（集中爆发，来自 192.168.20.1）
09:xx  18 次 client disconnect，账号池压力开始出现（18 次池失败）
10:xx–12:xx  账号池间歇性失败，相似度拒绝持续（11:xx 32次 similarity）
13:xx–14:xx  压力上升：114 次池耗尽，20+50 次 client disconnect
14:xx  相似度拒绝峰值（33 次）
17:00–18:00  ⚠️ 重大高峰：429 次账号池失败，194 次 client disconnect，
             635 次慢请求；李居昌 Key 在 17:41 集中触发 52 次失败
18:00–19:00  缓慢恢复：66 次池失败，57 次 client disconnect，897 次慢请求
             （慢请求滞后 1h 是因为仍在排队）
19:xx–21:xx  趋于正常，各指标回落个位数
22:02  窗口结束
```

---

## 六、未触发的事件

| 事件 | 状态 |
|---|---|
| markAccountError (unauthorized/blocked) | ❌ 未触发 |
| markAccountRateLimited | ❌ 未触发 |
| Token refresh failed | ❌ 未触发 |
| 共享池耗尽通知 | ❌ 未触发 |
| 管理员 webhook (accountAnomaly) | ❌ 未触发 |

> 判断：所有账号池耗尽均由 temp-退避 + 自动恢复机制在服务内吸收，无 webhook 上报；这是一个已知的可观测性盲区——管理员在峰值期间无法收到主动告警。

---

## 七、建议改进项

| 优先级 | 项目 |
|---|---|
| 🔴 高 | "Others" 组账号容量不足以承载 17:00 峰值，考虑增加账号或对 Top-4 受影响 Key 设置并发上限 |
| 🔴 高 | 账号池耗尽事件无 webhook，峰值期间管理员无感知；建议在 500 超阈值（如 >50/分钟）时触发 accountAnomaly 通知 |
| 🟡 中 | 账号 `129869b2`（UUID）API 请求错误 52 次，占 API 错误的 93%，需排查该账号稳定性或配置问题 |
| 🟡 中 | 慢请求 avg 22s / max 617s，客户端排队时长已影响体验；可考虑为 Others 组设置排队超时上限后直接 429，而非无限等待 |
| 🟢 低 | 249 次相似度拒绝全天散布，Key 字段为空，建议排查是否有未授权客户端绕过认证到达相似度检查 |

---

**底线：17:00–18:00 的单小时高峰完全压垮了"Others"账号池，产生 429 次选账号失败和 10 分钟级慢请求，全程无上游故障、无账号封禁，核心风险是容量与可观测性缺口。**
